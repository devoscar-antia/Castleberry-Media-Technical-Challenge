/**
 * Single source of truth for app version across all platforms
 * Update this file to change version everywhere (web, iOS, Android)
 *
 * After updating, run: npm run version:sync
 */

export const APP_VERSION = "2.0.9";
export const BUILD_NUMBER = 47;
